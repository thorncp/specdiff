#!/usr/bin/env bash

set -e

grep_pattern="describe\|it\|context\|specify"

diff=${DIFF:-diff}

$diff -b -U 3 \
  <(git grep -w -h --no-line-number --no-color -e $grep_pattern ${1:-master} -- spec/) \
  <(git grep -w -h --no-line-number --no-color -e $grep_pattern ${2:-HEAD} -- spec/)
